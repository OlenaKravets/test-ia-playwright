# Playwright + Python Test Framework з використанням POM

Мінімальний тестовий фреймворк на основі Playwright та Python з реалізацією паттерну Page Object Model (POM).

## 📁 Структура проекту

```
my_test_project/
├── conftest.py                  # Pytest конфігурація та фікстури
├── pages/                       # Папка з Page Objects
│   ├── __init__.py
│   ├── base_page.py            # Базовий клас для всіх сторінок
│   ├── login_page.py           # Page Object для сторінки логіну
│   └── registration_page.py    # Page Object для сторінки реєстрації
└── tests/                       # Папка з тестами
    ├── __init__.py
    ├── test_login.py           # Тести для функціональності логіну
    └── test_registration.py    # Тести для функціональності реєстрації
```

## 📝 Опис папок та файлів

### `conftest.py` - Конфігурація тестів
Файл містить pytest фікстури для ініціалізації та управління браузером:
- **`browser()`** - фікстура для запуску Chromium браузера
- **`page()`** - фікстура для створення нової сторінки для кожного тесту

### `pages/` - Page Objects

#### `base_page.py` - Базовий Page Object
Містить базовий клас `BasePage` з основними методами для взаємодії зі сторінками:
- `navigate(url)` - перехід на URL
- `click(selector)` - клік на елемент
- `fill(selector, text)` - заповнення поля вводу
- `get_text(selector)` - отримання тексту елемента
- `is_visible(selector)` - перевірка видимості елемента
- `get_content()` - отримання вмісту сторінки

#### `login_page.py` - Page Object для логіну
Клас `LoginPage` наслідує `BasePage` і містить методи для взаємодії зі сторінкою логіну:
- `open()` - відкриття сторінки логіну
- `login(username, password)` - виконання логіну
- `is_login_successful()` - перевірка успішного логіну
- `is_error_displayed()` - перевірка наявності помилки
- `get_error_message()` - отримання тексту помилки

**Тестова сторінка:** https://practicetestautomation.com/practice-test-login/

#### `registration_page.py` - Page Object для реєстрації
Клас `RegistrationPage` наслідує `BasePage` і містить методи для взаємодії зі сторінкою реєстрації:
- `open()` - відкриття сторінки реєстрації
- `register(...)` - виконання реєстрації з всіма параметрами
- `is_registration_successful()` - перевірка успішної реєстрації
- `is_error_displayed()` - перевірка наявності помилки

**Тестова сторінка:** https://practicetestautomation.com/practice-test-registration/

### `tests/` - Тести

#### `test_login.py` - Тести логіну
Клас `TestLogin` містить два тести:

1. **`test_successful_login()`** - тест успішного логіну
   - Відкриває сторінку логіну
   - Вводить валідні облікові дані (username: `student`, password: `Password123`)
   - Перевіряє, що логін виконаний успішно

2. **`test_login_with_wrong_password()`** - тест логіну з неправильним паролем
   - Відкриває сторінку логіну
   - Вводить валідне ім'я користувача та неправильний пароль
   - Перевіряє, що відображається повідомлення про помилку

#### `test_registration.py` - Тести реєстрації
Клас `TestRegistration` містить два тести:

1. **`test_registration_page_opens()`** - тест відкриття сторінки реєстрації
   - Перевіряє, що сторінка реєстрації відкривається
   - Проверяє наявність слова "registration" в URL

2. **`test_registration_form_elements_visible()`** - тест наявності форми
   - Перевіряє, що сторінка реєстрації завантажується
   - Перевіряє наявність вмісту на сторінці

## 🚀 Використання

### Інсталяція залежностей
```bash
pip install playwright pytest
playwright install chromium
```

### Запуск всіх тестів
```bash
pytest tests/ -v
```

### Запуск конкретного тесту
```bash
pytest tests/test_login.py::TestLogin::test_successful_login -v
```

### Запуск тестів з более детальним виводом
```bash
pytest tests/ -v -s
```

## 📊 Результат запуску тестів

```
tests/test_login.py::TestLogin::test_successful_login PASSED            [ 25%]
tests/test_login.py::TestLogin::test_login_with_wrong_password PASSED   [ 50%]
tests/test_registration.py::TestRegistration::test_registration_page_opens PASSED [ 75%]
tests/test_registration.py::TestRegistration::test_registration_form_elements_visible PASSED [100%]

============================== 4 passed in 8.34s ==============================
```

## 🎯 Паттерн Page Object Model (POM)

Фреймворк використовує паттерн POM для забезпечення:
- **Модульності** - кожна сторінка інкапсульована в окремий класс
- **Переиспользуемости** - методи Page Objects можуть використовуватися в різних тестах
- **Легкості обслуговування** - зміни в селекторах виконуються в одному місці
- **Читабельності** - тести описують дії користувача на зрозумілій мові

## 🔧 Технічний стек

- **Playwright** - автоматизація браузера
- **Python 3.11+** - мова програмування
- **Pytest** - фреймворк для тестування
- **Chromium** - браузер для запуску тестів

## 📌 Тестові дані

- **Валідне ім'я користувача:** `student`
- **Валідний пароль:** `Password123`
- **Сторінка для тестування логіну:** https://practicetestautomation.com/practice-test-login/
- **Сторінка для тестування реєстрації:** https://practicetestautomation.com/practice-test-registration/

## 💡 Основні концепції

### BasePage
Базовий клас, який містить загальні методи для всіх Page Objects. Це дозволяє уникнути дублювання коду.

### LoginPage і RegistrationPage
Спеціалізовані Page Objects для конкретних сторінок. Вони наслідують `BasePage` та добавляють специфічні методи для кожної сторінки.

### Фікстури
Pytest фікстури автоматично керують браузером та сторінками для кожного тесту, забезпечуючи ізоляцію тестів.

## 📚 Додаткові ресурси

- [Документація Playwright](https://playwright.dev/python/)
- [Документація Pytest](https://docs.pytest.org/)
- [Page Object Model Pattern](https://www.selenium.dev/documentation/test_practices/encouraged/page_object_models/)

---

**Автор:** Test Automation Framework  
**Дата створення:** 2025-12-10  
**Версія:** 1.0
